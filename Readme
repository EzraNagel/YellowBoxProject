CSC 407 Project
Ethan Engert and Ezra Nagel

To run:
    python -m venv .venv
    source .venv/bin/activate
    pip install -r requirements.txt

Test database connection- returns all movies in database (currently empty):
    export FLASK_APP=app
    flask shell
    >>> from app import db, Movie, Rating
    >>> Movie.query.all()
    >>> Rating.query.all()
    >>> Credit.query.all()
